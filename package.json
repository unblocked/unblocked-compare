{
  "name": "unblocked-compare",
  "version": "0.1.0",
  "description": "Measure what Unblocked adds to coding agents (Claude Code, Cursor, Codex): an A/B compare tool and a context-engine simulator",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "bun src/index.ts",
    "compare": "bun src/index.ts",
    "simulate": "bun src/simulate/index.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
